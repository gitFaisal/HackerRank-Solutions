*----------------------------------------------------*
             Challenge: Mini-Max Sum
*----------------------------------------------------*

def miniMaxSum(arr):
    mini = sum(arr) - max(arr)
    maxx = sum(arr) - min(arr)
    print mini, maxx
